import AuthProvider from "@/components/AuthProvider"; // Make sure the path is correct

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>
        <AuthProvider>
          {children}
        </AuthProvider>
      </body>
    </html>
  );
}